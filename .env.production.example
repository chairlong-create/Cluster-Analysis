# 数据库文件路径（Docker 内部路径，不需要修改）
DATABASE_FILE=/data/cluster-analysis.db

# NextAuth 密钥（必须修改为随机字符串，至少 32 字符）
# 可以用命令生成：openssl rand -base64 32
AUTH_SECRET=your-random-secret-at-least-32-chars

# 首次启动时自动创建的管理员密码
ADMIN_PASSWORD=admin123
